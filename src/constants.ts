/**
 * Frontend constants mirroring the Rust engine constants.
 *
 * Must stay in sync with src-tauri/src/engine/constants.rs.
 */

export const MIN_INTERVAL_MS = 100;
export const MAX_INTERVAL_MS = 3_600_000; // 1 hour
export const PANIC_HOLD_MS = 1_000;
